/**
 * GpsData.ets — Shared GPS state object
 *
 * @Observed enables ArkUI reactivity: UI auto-updates when fields change.
 * CameraService writes; pages read via polling — no callback needed.
 */
@Observed
export class GpsData {
  latitude: number = 0;
  longitude: number = 0;
  altitude: number = 0;
  satellites: number = -1; // -1 = unknown; real count (>= 0) once GNSS reports
  hasFix: boolean = false;
  lastUpdateTime: string = '--';
  lastLocationAt: number = 0;
  lastLocationAccuracy: number = -1;
  lastLocationError: string = '';
}
